# 알고리즘 수강전 테스트

```python
# 변수
name = "홍길동"
print(name)

# 배열
fruits = ["딸기", "사과", "배"]
print(fruits[0])

# 반복문
for i in rarnge(10):
    print("홍길동")

fruits = ["딸기", "사과", "배"]
for fruit in fruits:
    print(fruit)

# 조건문
age = 29
if age >= 20:
    print("성인입니다")
else:
    print("성인이 아닙니다.")

if operating_hours >= 17 and operating_hours <=23:
    print("운영시간입니다.")

# 함수
customer1_age = 18
customer2_age = 30
customer3_age = 3
customer4_age = 88

def check_identication_card(age):
    if (age) >= 20:
        print("성인입니다.")
    else:
        print("성인이 아닙니다.")

check_identication_card(customer1_age)
check_identication_card(customer2_age)
check_identication_card(customer3_age)
check_identication_card(customer4_age)
```